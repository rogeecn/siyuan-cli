# 图文发布测试：指定 Markdown 文件

这是一篇通过 `--content-file` 发布的测试文档。

它的目的不是验证排版有多漂亮，而是验证：

- Markdown 文件内容能否完整发布
- 图片引用语法能否被原样写入文档
- 列表、段落和标题结构能否被保留

下面是一张本地生成的测试图片：

![](./assets/publish-test-image.svg)

如果这段内容能够被成功发布并回读出来，就说明“指定 Markdown 文件发布图文内容”这条链路至少是通的。
